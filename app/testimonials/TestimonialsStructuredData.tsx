/** Review schema is intentionally not emitted from client-fetched testimonials.
 * Only publish review markup after consent, visible verification and a manual audit. */
export const TestimonialsStructuredData = () => null;
