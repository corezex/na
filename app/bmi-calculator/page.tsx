"use client";import {useState} from "react";import Link from "next/link";export default function BMI(){const[h,setH]=useState("");const[w,setW]=useState("");const[r,setR]=useState<number|null>(null);function calc(e:React.FormEvent){e.preventDefault();const m=Number(h)/100,kg=Number(w);if(m>0&&kg>0)setR(kg/(m*m))}const category=r===null?"":r<18.5?"Below the standard adult BMI range":r<25?"Within the standard adult BMI range":r<30?"Above the standard adult BMI range":"Well above the standard adult BMI range";return <meta name="description" content="Calculate BMI and understand weight-management with DietFiniti practical guidance." />
<main className="bg-slate-50 px-4 py-16"><div className="mx-auto max-w-3xl"><p className="font-semibold text-teal-700">Informational tool</p><h1 className="mt-3 text-4xl font-bold text-slate-900">BMI Calculator for Adults</h1><p className="mt-5 leading-8 text-slate-700">Calculate body mass index (BMI) using height and weight. BMI is a screening measure for most adults; it is not a diagnosis and does not account for every factor affecting health.</p><form onSubmit={calc} className="mt-8 grid gap-5 rounded-2xl bg-white p-6 shadow-sm"><label className="grid gap-2 font-semibold">Height in centimetres<input required min="1" type="number" value={h} onChange={e=>setH(e.target.value)} className="rounded-lg border p-3 font-normal"/></label><label className="grid gap-2 font-semibold">Weight in kilograms<input required min="1" type="number" value={w} onChange={e=>setW(e.target.value)} className="rounded-lg border p-3 font-normal"/></label><button className="rounded-lg bg-teal-700 px-5 py-3 font-semibold text-white">Calculate BMI</button></form>{r!==null&&<section className="mt-8 rounded-2xl border border-teal-100 bg-teal-50 p-6" aria-live="polite"><h2 className="text-2xl font-bold">Your BMI: {r.toFixed(1)}</h2><p className="mt-3 font-semibold text-teal-900">{category}</p><p className="mt-4 leading-7 text-slate-700">This result is not personalised medical advice. BMI may be less useful in pregnancy, for children and teenagers, older adults, athletes, and people with particular health circumstances. Speak with a qualified clinician for interpretation in your context.</p></section>}<section className="mt-10"><h2 className="text-3xl font-bold">What BMI can and cannot tell you</h2><p className="mt-4 leading-8 text-slate-700">BMI compares weight with height. It does not measure body composition, nutrition status, fitness, medical history or overall wellbeing. Avoid using one number alone to make major diet, exercise or treatment decisions.</p><Link href="/contact" className="mt-6 inline-block font-semibold text-teal-800 underline">Talk to DietFiniti about nutrition support</Link></section></div></main>}
